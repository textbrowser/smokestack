Completed Items

- Allow removal of messages per participant.
- Create a single statement in purgeReleasedMessages() to delete specific OIDs.
- Decrease the lock area of m_stringBuilder in Neighbor.java.
- Discovery via cryptography.
- Display the number of internal Neighbors.
- Echo shared Smoke identities.
- Entries in routing_identities expire.
- Fire compatibility.
- Listeners.
- Message accounting.
- Optional signatures on shared key pairs.
- Per-participant message retrieval schedulers.
- Populate containers in the kernel as required.
- Private servers. SHA-512 (Random || Time).
- Purge expired messages.
- Refresh Ozones.
- Rename participant in Participants section.
- Replace SipHash ID with Smoke ID.
- Share-identity confirmation.
- Signed release.
- m_isValidCertificate should be an AtomicBoolean.

Remaining Items

- Abnormal native termination on Android 8.x. SSL/TLS and/or libc.
- Dialogs are not dismissed correctly on rotation changes.
- Distribute routing identity algorithms.
- Private servers are susceptible to replay attacks.
- Steam.
- The Settings activity has many views (ANDROID_LINT_MAX_VIEW_COUNT).
